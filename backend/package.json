{
  "name": "gerenciamento-usuarios-fullstack",
  "version": "1.0.0",
  "description": "Uma API completa para gerenciamento de usuários.",
  "main": "index.js",
  "type": "module",
  "license": "ISC",
  "scripts": {
    "start": "node src/index.js",
    "build": "prisma generate && prisma migrate deploy",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon src/index.js"
  },
  "dependencies": {
    "@prisma/client": "^6.12.0",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "prisma": "^6.12.0"
  }
}
